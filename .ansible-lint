skip_list:
    - ignore-errors
    - internal-error
    - latest[git]
    - meta-no-tags
    - name[template]
    - no-changed-when
    - partial-become[task]
    - role-name[path]
    - schema[meta]
